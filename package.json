{
  "name": "interactive-readme-pro",
  "version": "1.1.0",
  "description": "The ultimate documentation workflow tool for professional developers, teams, and organizations.",
  "main": "app.js",
  "bin": {
    "readme-pro": "./cli.js"
  },
  "scripts": {
    "test": "node --no-warnings ./node_modules/jest/bin/jest.js",
    "build": "node build.js",
    "prepare": "node scripts/prepare.js",
    "clean": "node -e \"require('fs').unlink('cli.js', (err) => { if (err && err.code !== 'ENOENT') throw err; })\""
  },
  "keywords": [
    "readme",
    "generator",
    "ai",
    "gemini",
    "openai",
    "documentation"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "node-fetch": "^2.7.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  }
}